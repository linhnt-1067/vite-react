export enum AuthenStorageKeys {
  ACCESS_TOKEN = 'accessToken',
  AUTH_STATE_ID = 'authStateId',
}

export enum MessageStatus {
  INFO = 'info',
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  LOADING = 'loading',
}
